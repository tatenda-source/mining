# Requirements for foundation model fine-tuning (Prithvi-EO-2.0 / Clay)
# -----------------------------------------------------------------------
# IMPORTANT: These require PyTorch. Install PyTorch first for your platform:
#   CPU (macOS Apple Silicon):  pip install torch torchvision
#   GPU (CUDA 12.x):           pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
#
# GDAL is also required. Best installed via conda:
#   conda install -c conda-forge gdal
# -----------------------------------------------------------------------

# -- Core framework for Prithvi fine-tuning --
terratorch>=1.0.0
lightning>=2.2.0
torchgeo>=0.6.0
timm>=1.0.0
segmentation-models-pytorch>=0.3.4
einops>=0.8.0

# -- HuggingFace ecosystem (model download, configs) --
huggingface-hub>=0.23.0
safetensors>=0.4.0
transformers>=4.40.0

# -- Geospatial I/O (likely already installed from main project) --
rasterio>=1.3
rioxarray>=0.15
geopandas>=0.14

# -- Clay Foundation Model (alternative to Prithvi) --
# Install from git:  pip install git+https://github.com/Clay-foundation/model.git
# Uncomment below if using Clay:
# clay-model

# -- Visualization --
matplotlib>=3.8

# -- Utilities --
tqdm>=4.66
pyyaml>=6.0
